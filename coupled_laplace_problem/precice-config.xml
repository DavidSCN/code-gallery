<?xml version="1.0"?>

<precice-configuration>

    <log>
        <sink filter="%Severity% > debug and %Rank% = 0" format="---[precice] %ColorizedSeverity% %Message%" enabled="true"/>
    </log>

    <solver-interface dimensions="2">

   <data:scalar name="boundary-data"/>

    <mesh name="original-mesh">
        <use-data name="boundary-data"/>
    </mesh>

    <mesh name="dummy-mesh">
    <use-data name="boundary-data"/>
    </mesh>

    <participant name="laplace-solver">
        <use-mesh name="original-mesh" provide="yes"/>
        <use-mesh name="dummy-mesh" from="dummy-participant"/>
        <read-data name="boundary-data" mesh="original-mesh"/>
        <mapping:rbf-thin-plate-splines direction="read" from="dummy-mesh" to="original-mesh" constraint="consistent" x-dead="true"/>
    </participant>

    <participant name="dummy-participant">
        <use-mesh name="dummy-mesh" provide="yes"/>
        <write-data name="boundary-data" mesh="dummy-mesh"/>
    </participant>

    <m2n:sockets from="laplace-solver" to="dummy-participant" />

    <coupling-scheme:serial-explicit>
        <time-window-size value="1"/>
        <max-time value="10"/>
        <participants first="laplace-solver" second="dummy-participant"/>
        <exchange data="boundary-data" mesh="dummy-mesh" from="dummy-participant" to="laplace-solver" initialize="true"/>
    </coupling-scheme:serial-explicit>

    </solver-interface>
</precice-configuration>
